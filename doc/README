REQUIREMENTS for INSTALLATION 
=============================

- root privileges

- Apache with mod_rewrite installed. It must be configured to work in
  .htaccess files.

- mysql server and the client library.

- openssl library.

- xinetd (or similar).

- ragel (if building from the source repository).

INSTALLATION
============

See the INSTALL file.
